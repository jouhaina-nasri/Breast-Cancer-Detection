This folder contains the trained convolutional neural network (CNN) model
used by the Breast Cancer Detection web application.

The model is loaded in app.py at runtime to perform predictions on uploaded
breast cancer images (benign vs malignant).
